import Loading from 'react-loading'

const LoadingElement = () => {
     return (
          <div className='h-full'>
               <Loading type="cylon" color="#1E1E1E" height={50} width={50} />
          </div>
     )
}

export default LoadingElement